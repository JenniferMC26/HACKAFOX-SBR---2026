{
  "name": "paso-backend",
  "description": "Scripts de seed y utilidades — no es la Cloud Function",
  "private": true,
  "scripts": {
    "seed:nodes": "node seed/supabase-seed.js",
    "seed:users": "node seed/users-seed.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
