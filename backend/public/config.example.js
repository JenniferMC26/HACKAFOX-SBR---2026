// Copiar este archivo a `config.js` y rellenar con tus propias claves.
// `config.js` está en .gitignore — NO commitear.
//
// Claves públicas — seguras para el browser (RLS las protege en Supabase y
// la Maps key debe estar restringida por dominio en Google Cloud Console).
// NUNCA incluir SUPABASE_SERVICE_KEY aquí.
window.PASO_CONFIG = {
  SUPABASE_URL:        'https://<tu-proyecto>.supabase.co',
  SUPABASE_ANON_KEY:   '<tu-anon-key>',
  GOOGLE_MAPS_API_KEY: '<tu-maps-js-api-key>',
  GEMINI_API_KEY:      '<tu-gemini-key>',
};
